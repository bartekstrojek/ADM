import styled from 'styled-components';

export const SectionShadow = styled.div`
    background: radial-gradient(40% 50% at 50% 50%, rgb(255 255 255 / 44%) 0%, rgb(0 0 0 / 0%) 100%);
    bottom: 0;
    flex: none;
    height: 4px;
    left: 0;
    position: absolute;
    right: 0;
    z-index: 1;
`;